external class ZLibCodec {
    construct 
}
external class GZipCodec {
    construct 
}
external class ZLibEncoder {
    construct 
    get gzip
    get level
    get memLevel
    get strategy
    get windowBits
    get dictionary
    get raw
    fun convert
    fun startChunkedConversion
}
external class ZLibDecoder {
    construct 
    get windowBits
    get dictionary
    get raw
    fun convert
    fun startChunkedConversion
}
