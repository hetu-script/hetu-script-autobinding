external class Rectangle {
    construct 
    get left
    get top
    get width
    get height
}
external class MutableRectangle {
    construct 
    var left
    var top
    get width
    set width(value)
    get height
    set height(value)
}
