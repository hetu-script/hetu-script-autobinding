external class BorderStyle {
    static var none
    static var solid
}
external class BorderSide {
    construct 
    get color
    get width
    get style
    fun copyWith
    fun scale
    fun toPaint
    get hashCode
    fun toString
    static fun merge (a, b)
    static fun canMerge (a, b)
    static fun lerp (a, b, t)
    static get none
}
