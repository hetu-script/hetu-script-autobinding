external class Theme {
    construct 
    get data
    get child
    fun build
    fun debugFillProperties
    static fun of (context)
}
external class ThemeDataTween {
    construct 
    fun lerp
}
external class AnimatedTheme {
    construct 
    get data
    get child
    fun createState
}
