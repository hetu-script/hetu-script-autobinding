external class TabController {
    construct 
}
external class DefaultTabController {
    construct 
    get length
    get initialIndex
    get child
    fun createState
    static fun of (context)
}
