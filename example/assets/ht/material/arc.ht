external class MaterialPointArcTween {
    construct 
    get center
    get radius
    get beginAngle
    get endAngle
    set begin(value)
    set end(value)
    fun lerp
    fun toString
}
external class MaterialRectArcTween {
    construct 
    get beginArc
    get endArc
    set begin(value)
    set end(value)
    fun lerp
    fun toString
}
external class MaterialRectCenterArcTween {
    construct 
    get centerArc
    set begin(value)
    set end(value)
    fun lerp
    fun toString
}
